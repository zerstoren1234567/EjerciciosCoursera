<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.curso.coursera">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Coursera">
        <activity android:name=".ContactoActivity"></activity>
        <activity android:name=".AboutActivity" />
        <activity
            android:name=".MainEjercicio3Activity"
            android:label="@string/title_activity_main_ejercicio3"
            android:theme="@style/Theme.Coursera.NoActionBar" />
        <activity android:name=".Ejercicio2Activity" />
        <activity android:name=".RankActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>